# Скрипты для работы с проектом

## seed-database.ts

Скрипт для заполнения базы данных тестовыми данными.

### Что делает скрипт:

- ✅ Создает 10 типов рецептов (если их нет)
- ✅ Создает 100+ продуктов с реальными калориями
- ✅ Создает 50 рецептов с полной информацией:
  - Название, описание
  - Тип рецепта
  - Калории и время приготовления
  - Связанные продукты
  - Пошаговые инструкции с ресурсами

### Использование:

```bash
# Запуск скрипта
npm run seed
```

### Требования:

1. База данных PostgreSQL должна быть запущена и доступна
2. Переменные окружения должны быть настроены в `.env` или `.env.local`:
   ```
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=constructor_auth
   DB_USER=postgres
   DB_PASSWORD=your_password
   ```

### Особенности:

- Скрипт проверяет существование записей перед созданием (не создает дубликаты)
- Использует реальные данные о калориях продуктов
- Создает реалистичные рецепты с пошаговыми инструкциями
- Автоматически связывает рецепты с продуктами
- Выводит подробную статистику по завершении

### Пример вывода:

```
🌱 Начало заполнения базы данных...

📊 Конфигурация БД:
   Host: localhost
   Port: 5432
   Database: constructor_auth
   User: postgres

✓ Подключение к базе данных установлено

📝 Создание типов рецептов...
  ✓ Завтрак
  ✓ Обед
  ✓ Ужин
  ...

✓ Создано/найдено 10 типов рецептов

🥬 Создание продуктов...
  ✓ Создано новых продуктов: 100
  ✓ Найдено существующих продуктов: 0
  ✓ Всего продуктов в базе: 100

🍳 Создание рецептов...
  ✓ Омлет с овощами (Завтрак)
  ✓ Салат Цезарь (Салат)
  ...

✓ Создано новых рецептов: 50
✓ Найдено существующих рецептов: 0
✓ Всего рецептов в базе: 50

✅ Заполнение базы данных завершено успешно!

📊 Итоговая статистика:
   - Типы рецептов: 10
   - Продукты: 100
   - Рецепты: 50

✓ Соединение с базой данных закрыто

🎉 Скрипт выполнен успешно!
```

---

## fetch-swagger.js

Скрипт для получения Swagger JSON из работающего backend.

### Использование:

```bash
# Базовое использование
npm run swagger:fetch

# С красивым форматированием
npm run swagger:fetch:pretty

# С кастомными параметрами
node scripts/fetch-swagger.js --url http://localhost:3000 --output swagger.json --pretty
```

### Параметры:

- `--url <url>` - URL backend сервера (по умолчанию: http://localhost:3000)
- `--output <path>` - Путь для сохранения JSON (по умолчанию: swagger.json)
- `--pretty` - Красивое форматирование JSON
- `--help, -h` - Показать справку

